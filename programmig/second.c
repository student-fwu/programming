#include<stdio.h>
int main()
{
    system("cls");
    int x, p=2, q=4;
    float t=9.7;
    x = p+q+(int)t;
    printf("data in int format=%d",x);
}
// debug garna sikha laa erors dherai aaira xan aab aru kae problem vaye vana ma aab exit gardai xuuu ok bye see u at 

    // where is preprocessor directives ??